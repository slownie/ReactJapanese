import * as React from 'react';
import { SafeAreaView, ScrollView, Text } from 'react-native';

import styles from '../Styles';

function VerbConj() {
    return (
        <SafeAreaView style = {styles.page}>
            <ScrollView>

            </ScrollView>
        </SafeAreaView>
    );
}

export default VerbConj;